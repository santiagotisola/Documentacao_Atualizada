import PipelineEditais from '../../PipelineEditais';

// Pipeline stub substituído pelo componente funcional completo
// (4 etapas: Análise → Revisão → Roadmap → Specs)
const Pipeline = ({ metricas, setMetricas }) => <PipelineEditais />;
export default Pipeline;